import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>🚀 Arbiboard build is working!</h1>;

ReactDOM.createRoot(document.getElementById('root')).render(<App />);