'use client';
export default function AIModulePlaygroundPanel({ id }: { id: string }) {
  return (
    <div>
      <h2>Playground</h2>
      <p>Sandbox per testare input/output AI.</p>
    </div>
  );
}
