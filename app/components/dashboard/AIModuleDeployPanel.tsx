'use client';
export default function AIModuleDeployPanel({ id }: { id: string }) {
  return (
    <div>
      <h2>Deploy</h2>
      <button>Deploy strategia</button>
      <p>Stato: pronto</p>
    </div>
  );
}
